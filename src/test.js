'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('waves6.js');

    describe('Constructor', function () {
        it('should construct an object with data members', function () {
            var wave = new Wave();
            expect(wave).to.have.any.keys('a1');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should construct an object with data members', function () {
            var wave = new Wave();
            expect(wave).to.have.any.keys('a1');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should construct an object with data members', function () {
            var wave = new Wave();
            expect(wave).to.have.any.keys('a1');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should construct an object with data members', function () {
            var wave = new Wave();
            expect(wave).to.have.any.keys('a1');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should construct an object with data members', function () {
            var wave = new Wave();
            expect(wave).to.have.any.keys('a1');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should construct an object with data members', function () {
            var wave = new Wave();
            expect(wave).to.have.any.keys('a1');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should construct an object with data members', function () {
            var wave = new Wave();
            expect(wave).to.have.any.keys('a1');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should construct an object with data members', function () {
            var wave = new Wave();
            expect(wave).to.have.any.keys('obj');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should construct an object with data members', function () {
            var wave = new Wave();
            expect(wave).to.have.any.keys('obj');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should take an options object', function () {
            var wave = new Wave({
                amp1: 3,
                amp2: 5,
                freq1: 4,
                freq1: 5
            });
            expect(wave).to.have.any.keys('data');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('../src/waves6.js');

    describe('Constructor', function () {
        it('should take an options object', function () {
            var wave = new Wave({
                amp1: 3,
                amp2: 5,
                freq1: 4,
                freq1: 5
            });
            expect(wave).to.have.any.keys('data');
        });
    });
});
'use strict';

var expect = require('chai').expect;
var assert = require('assert');

describe('Controllers/Wave.js', function () {

    var Wave = require('src/waves6.js');

    describe('Constructor', function () {
        it('should take an options object', function () {
            var wave = Wave({
                amp1: 3,
                amp2: 5,
                freq1: 4,
                freq1: 5
            });
            expect(wave).to.have.any.keys('data');
        });
    });
});