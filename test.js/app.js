'use strict';

var expect = require('chai').expect;
var assert = require('assert');
describe("Should return true", function () {
  it('should show true equals true', function () {
    assert.equal(true, true);
  });
});